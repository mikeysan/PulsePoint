name: 🚀 Deploy PulsePoint

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Production Server
    runs-on: ubuntu-latest
    environment: remote-connection

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🔐 Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY }}

      - name: 🧠 Verify SSH Connection
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.DEV_SERVER }} >> ~/.ssh/known_hosts
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.DEV_SERVER }} "echo '✅ Connected to production server'"

      - name: 🚀 Run deploy script
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.DEV_SERVER }} "cd /opt/webapps/pulsepoint && chmod +x && ./deploy.sh"
